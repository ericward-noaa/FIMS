% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fims_model.R
\name{fims_model}
\alias{fims_model}
\alias{fims_parameters}
\alias{fims_recruitment}
\alias{fims_growth}
\alias{fims_maturity}
\alias{fims_depletion}
\alias{fims_observations}
\title{Build a FIMS model from data and components}
\usage{
fims_model(data)

fims_parameters(model, parameters)

fims_recruitment(
  model,
  form = c("BevertonHolt"),
  process_distribution = "Dnorm"
)

fims_growth(model, form = c("EWAA"))

fims_maturity(model, form = c("Logistic"))

fims_depletion(
  model,
  form = c("PellaTomlinson"),
  process_distribution = "Dnorm"
)

fims_observations(
  model,
  fleet = NULL,
  selectivity = NULL,
  landings = "Dlnorm",
  index = "Dlnorm",
  age_comp = "Dmultinom",
  length_comp = "Dmultinom"
)
}
\arguments{
\item{data}{A data frame or \code{\link[=FIMSFrame]{FIMSFrame()}} object.}

\item{model}{A model builder object returned from \code{\link[=fims_model]{fims_model()}}.}

\item{parameters}{A parameter tibble, typically edited from
\code{model$parameters} after model components have been added.}

\item{form}{A character string naming a structural form, such as
\code{"BevertonHolt"}, \code{"EWAA"}, \code{"Logistic"}, or \code{"PellaTomlinson"}.}

\item{process_distribution}{A character string naming the process
distribution for a dynamic component, or \code{NA_character_} for no process
distribution.}

\item{fleet}{A character string naming the fleet or survey to configure.
When the data contain one fleet, it can be omitted.}

\item{selectivity}{A character string naming the selectivity form for
\code{fleet}. If omitted, age-structured models use \code{"Logistic"} and
surplus-production models do not add selectivity.}

\item{landings, index, age_comp, length_comp}{Character strings naming data
likelihood distributions for this fleet's observation components.}
}
\value{
A model builder object that can be initialized with
\code{\link[=initialize_fims]{initialize_fims()}}.
}
\description{
\code{fims_model()} starts a model from data. Add components with native R pipes
so each component updates and returns the model object.
}
\examples{
\dontrun{
data("data_big", package = "FIMS")
data_4_model <- FIMSFrame(data_big)

model <- fims_model(data_4_model) |>
  fims_growth() |>
  fims_recruitment() |>
  fims_maturity() |>
  fims_observations(
    fleet = "fleet1",
    selectivity = "DoubleLogistic"
  )
input <- initialize_fims(model)
}
}
