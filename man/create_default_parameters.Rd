% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_default_parameters.R
\name{create_default_parameters}
\alias{create_default_parameters}
\title{Create default parameters for a FIMS model}
\usage{
create_default_parameters(parameter_plan, data)
}
\arguments{
\item{parameter_plan}{A model created by \code{\link[=fims_model]{fims_model()}} or an internal
parameter plan.}

\item{data}{A \code{FIMSFrame} object returned from running \code{\link[=FIMSFrame]{FIMSFrame()}} on
your long input data.}
}
\value{
A nested \code{tibble} containing information on parameters for your model with
the same top-level columns as the input tibble passed to \code{parameter_plan}
but with additional information in the nested \code{data} column. See below for
more details:
\describe{
\item{\code{model_family}:}{The specified model family (e.g.,
"catch_at_age").}
\item{\code{module_name}:}{The name of the FIMS module (e.g.,
"Data", "Selectivity", "Recruitment", "Growth", "Maturity"). These
entries are always written in PascalCase to match the names used in the
C++ code.}
\item{\code{fleet_name}:}{The name of the fleet the module applies to. This
will be \code{NA} for non-fleet-specific modules like "Recruitment".}
\item{\code{data}:}{A list-column containing a \code{tibble} with detailed
parameters. Unnesting this column reveals:
\describe{
\item{\code{module_type}:}{The specific type of the module (e.g.,
"Logistic" for a "Selectivity" module). This column will always be
written in PascalCase to match the names used in the C++ code.}
\item{\code{label}:}{The name of the parameter (e.g.,
"inflection_point").}
\item{\code{age}:}{The age the parameter applies to.}
\item{\code{length}:}{The length bin the parameter applies to.}
\item{\code{time}:}{The time step (i.e., year) the parameter applies
to.}
\item{\code{value}:}{The initial value of the parameter.}
\item{\code{estimation_type}:}{The type of estimation (e.g.,
"constant", "fixed_effects", "random_effects").}
\item{\code{distribution_type}:}{The type of distribution (e.g.,
"Data", "process"), where a process distribution can refer to a
fixed effect or a random effect but it does not fit to data, e.g.,
recruitment deviations.}
\item{\code{distribution}:}{The name of distribution (e.g.,
"Dlnorm", \code{Dmultinom}). The column will always be written in
PascalCase to match the names used in the C++ code.}
}
}
}
}
\description{
This function returns the parameter tibble needed to run a FIMS model. When
passed a model created with \code{\link[=fims_model]{fims_model()}}, it returns the model's current
parameter tibble. You can edit that tibble if you want to change initial
values or estimation settings before running your model.
}
\details{
The function processes an internal parameter plan created from model
components by calling helper functions on each row and returning a multi-row
parameter set for each input row. For example, if a selectivity component for
the first fleet uses \code{"Logistic"}, it takes that single row of input
information and returns a parameter set with two rows, one for each
parameter, \code{"inflection_point"} and \code{"slope"}.
}
\examples{
\dontrun{
# Load the example dataset and create a FIMS data frame
data("data_big", package = "FIMS")
fims_frame <- FIMSFrame(data_big)

model <- fims_model(fims_frame) |>
  fims_growth() |>
  fims_recruitment() |>
  fims_maturity() |>
  fims_observations(fleet = "fleet1")

default_parameters <- model$parameters |>
  tidyr::unnest(cols = data)

# Update selectivity parameters for survey1
updated_parameters <- default_parameters |>
  dplyr::rows_update(
    tibble::tibble(
      fleet_name = "survey1",
      label = c("inflection_point", "slope"),
      value = c(1.5, 2)
    ),
    by = c("fleet_name", "label")
  )

}
}
\seealso{
\itemize{
\item \code{\link[=FIMSFrame]{FIMSFrame()}}
}
}
